subinclude("//tools:helper-tool")

def helper_rule(name: str):
    build_rule(
        name = name,
        cmd = "sh $TOOL > $OUT",
        tools = ["//tools:helper-tool"],
        outs = [name + ".out"],
    )
